---
import DestinationContent from "./DestinationContent.astro";
import { ui, defaultLang } from '../../i18n/ui';

// Get current language
const currentPath = Astro.url.pathname;
const lang = (Astro.currentLocale || (currentPath.startsWith('/en') ? 'en' : defaultLang)) as keyof typeof ui;

// Translation helper
function t(key: keyof typeof ui[typeof lang]) {
  return ui[lang][key];
}
---

<DestinationContent
  title={t('indonesia.page.title')}
  description={t('indonesia.page.desc')}
  capital={t('indonesia.page.capital')}
  language={t('indonesia.page.language')}
  currency={t('indonesia.page.currency')}
  imageSrc="/images/indonesia/indo_main.jpg"
  imageAlt="Beautiful landscape in Indonesia"
  planUrl={lang === 'nl' ? '/nl/route-planner?country=indonesia' : '/en/route-planner?country=indonesia'}
  guideUrl={lang === 'nl' ? '/eguides/' : '/en/eguides/'}
>
</DestinationContent>